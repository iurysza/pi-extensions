{
  "name": "openspec-review",
  "description": "Validation gates + verified fix loop — tests + spec validation + 4-angle review panel (spec/bugs/simplicity/security, 2 models each) + tool-armed arbiter that verifies findings against the code + fix round with regression tests + a confirm gate that re-runs the fix until green (max 2 extra rounds). Reviews merge-base..working-tree, so committed and uncommitted work are both covered.",
  "strictInterpolation": true,
  "budget": {
    "maxUSD": 20
  },
  "args": {
    "change": {
      "description": "OpenSpec change id under openspec/changes/<id>"
    },
    "base": {
      "default": "",
      "description": "Base branch for the diff (merge-base). Default: auto-detect main/master; falls back to HEAD (uncommitted only)."
    },
    "verify": {
      "default": "( if [ -f justfile ] && command -v just >/dev/null 2>&1; then echo '[verify] just test'; just test; elif [ -f Cargo.toml ]; then echo '[verify] cargo test'; cargo test --workspace; elif [ -f go.mod ]; then echo '[verify] go test'; go test ./...; elif [ -f package.json ]; then echo '[verify] npm test'; npm test; elif [ -f pyproject.toml ]; then echo '[verify] pytest'; pytest; else echo '[verify] no test runner detected - pass verify=...'; fi ) 2>&1 | tail -60",
      "description": "Shell command for the build/test check. Default auto-detects the project type (just/cargo/go/npm/pytest). Override per project when the default picks wrong."
    }
  },
  "phases": [
    {
      "id": "build-test",
      "type": "script",
      "run": [
        "bash",
        "-c",
        "set -o pipefail; ({args.verify}); code=$?; echo; echo VERIFY_EXIT=$code; exit 0"
      ],
      "timeout": 300000
    },
    {
      "id": "spec-validate",
      "type": "script",
      "run": [
        "sh",
        "-c",
        "openspec validate {args.change} --strict 2>&1; echo VALIDATE_EXIT=$?; exit 0"
      ]
    },
    {
      "id": "review-rules",
      "type": "script",
      "run": [
        "bash",
        "-c",
        "cat <<'EOF'\n- Flag only issues introduced by THIS change (not pre-existing), discrete and actionable.\n- Verify every claim against the actual code with read/grep BEFORE reporting it. Every finding MUST cite file:line AND quote 1-3 lines copied VERBATIM from the file — the arbiter greps your quote; if it does not exist in the file, the finding is auto-rejected. No speculation.\n- Severity: [blocker] = broken behavior, security hole, data loss, or spec violation — must fix before merge. [major] = real problem, should fix. [minor] = nice to fix.\n- Untrusted input: always flag non-parametrized SQL, open redirects, unescaped output into HTML/shell, SSRF on user-supplied URLs.\n- Be brief: <=1 paragraph per finding, code snippets <=3 lines.\n- Report EVERY qualifying issue, not just the first. If clean, output: No findings.\n- End with a `Coverage:` list — every file from the FILES CHANGED manifest marked `reviewed` or `skipped: <reason>`. The diff paste excludes lockfiles/generated files and may be truncated; any manifest file missing from the paste must be read directly, never skipped silently.\nEOF\n[ -f REVIEW_GUIDELINES.md ] && { echo; echo 'Project review guidelines:'; cat REVIEW_GUIDELINES.md; }\nRULES_DIR=\"$HOME/.pi/agent/taskflows/openspec-review-rules\"\n[ -d \"$RULES_DIR\" ] || exit 0\nbase_ref=\"$1\"\nif [ -z \"$base_ref\" ]; then for b in main master; do git rev-parse -q --verify \"$b\" >/dev/null 2>&1 && { base_ref=$b; break; }; done; fi\nmb=$(git merge-base HEAD \"$base_ref\" 2>/dev/null) || mb=HEAD\nfiles=$({ git diff --name-only \"$mb\" 2>/dev/null; git ls-files --others --exclude-standard; })\ndocs=\"\"\nmatch() { echo \"$files\" | grep -qE \"$1\" && docs=\"$docs $2\"; }\nmatch '\\.(ts|tsx|js|jsx|mjs|cjs)$' typescript.md\nmatch '\\.rs$' rust.md\nmatch '\\.py$' python.md\nmatch '(^|/)package\\.json$' package-json.md\nmatch '(^|/)Cargo\\.toml$' cargo-toml.md\nmatch '^\\.github/.*\\.(yml|yaml)$' github-workflows.md\nfor f in $docs; do [ -f \"$RULES_DIR/$f\" ] && { echo; echo \"File-type checklist ($f) — applies only to matching files:\"; cat \"$RULES_DIR/$f\"; }; done\nexit 0",
        "bash",
        "{args.base}"
      ]
    },
    {
      "id": "diff",
      "type": "script",
      "run": [
        "bash",
        "-c",
        "base_ref=\"$1\"\nif [ -z \"$base_ref\" ]; then\n  for b in main master; do git rev-parse -q --verify \"$b\" >/dev/null 2>&1 && { base_ref=$b; break; }; done\nfi\nif [ -n \"$base_ref\" ] && mb=$(git merge-base HEAD \"$base_ref\" 2>/dev/null); then\n  echo \"BASE_SHA=$mb (merge-base with $base_ref; diff covers committed + uncommitted work since the branch point)\"\nelse\n  mb=HEAD\n  echo 'BASE_SHA=HEAD (no base branch found; diff covers uncommitted work only)'\nfi\necho '--- FILES CHANGED (coverage manifest — every reviewer must account for each file) ---'\ngit diff --name-status \"$mb\"\ngit ls-files --others --exclude-standard | sed 's/^/??\\t/'\necho '--- stat ---'\ngit diff --stat \"$mb\"\necho '--- diff (lockfiles/generated excluded; may be truncated at 150 kB — any manifest file missing below was cut, read it directly) ---'\ngit diff \"$mb\" -- . ':(exclude)*.lock' ':(exclude)package-lock.json' ':(exclude)pnpm-lock.yaml' ':(exclude)yarn.lock' ':(exclude)go.sum' ':(exclude)*.min.js' ':(exclude)*.min.css' ':(exclude)**/generated/**' | head -c 150000",
        "bash",
        "{args.base}"
      ]
    },
    {
      "id": "review-spec-claude",
      "type": "agent",
      "agent": "reviewer",
      "model": "anthropic/claude-opus-4-8",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md",
        "openspec/changes/{args.change}/tasks.md"
      ],
      "task": "Spec-compliance review of OpenSpec change `{args.change}`.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files (and their callers) directly instead of trusting the paste alone.\n\n{steps.diff.output}\n\nFor EVERY requirement and scenario in the delta specs under openspec/changes/{args.change}/specs/, verify there is implementation evidence (cite file:line). Also check every task in tasks.md is genuinely done, not just checked off.\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If fully compliant, output exactly: No findings."
    },
    {
      "id": "review-spec-kimi",
      "type": "agent",
      "agent": "reviewer",
      "model": "kimi-coding/k2p7",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md",
        "openspec/changes/{args.change}/tasks.md"
      ],
      "task": "Spec-compliance review of OpenSpec change `{args.change}`.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files (and their callers) directly instead of trusting the paste alone.\n\n{steps.diff.output}\n\nFor EVERY requirement and scenario in the delta specs under openspec/changes/{args.change}/specs/, verify there is implementation evidence (cite file:line). Also check every task in tasks.md is genuinely done, not just checked off.\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If fully compliant, output exactly: No findings."
    },
    {
      "id": "review-bugs-claude",
      "type": "agent",
      "agent": "reviewer",
      "model": "anthropic/claude-fable-5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Correctness review of this change. Optimize for RECALL — surface anything that could be a real bug; a second reviewer and an arbiter will filter noise. The attached proposal.md tells you the INTENT of the change — judge the code against it.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly. CRITICAL: for every changed function/signature, grep its callers and check they still work.\n\nWork plan: FIRST list the risk points in this diff (one line each), THEN investigate each with your tools before reporting findings.\n\n{steps.diff.output}\n\nHunt for: logic errors, inverted/off-by-one conditions, unhandled edge cases (empty, null, unicode, huge input), broken error handling that swallows or misroutes failures, race conditions, resource leaks, API misuse, and cross-file breakage (callers of changed code, stale imports, dead references).\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If clean, output exactly: No findings."
    },
    {
      "id": "review-bugs-gpt",
      "type": "agent",
      "agent": "reviewer",
      "model": "openai-codex/gpt-5.5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Correctness review of this change. Optimize for PRECISION — report only bugs you are confident are real; for each one, explain the exact input/state that triggers it. The attached proposal.md tells you the INTENT of the change — judge the code against it.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly. CRITICAL: for every changed function/signature, grep its callers and check they still work.\n\nWork plan: FIRST list the risk points in this diff (one line each), THEN investigate each with your tools before reporting findings.\n\n{steps.diff.output}\n\nHunt for: logic errors, inverted/off-by-one conditions, unhandled edge cases (empty, null, unicode, huge input), broken error handling that swallows or misroutes failures, race conditions, resource leaks, API misuse, and cross-file breakage (callers of changed code, stale imports, dead references).\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If clean, output exactly: No findings."
    },
    {
      "id": "review-kiss-gpt",
      "type": "agent",
      "agent": "critic",
      "model": "openai-codex/gpt-5.5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Over-engineering review of this change. Scope: complexity ONLY — correctness and security belong to other reviewers. The attached proposal.md defines the requested scope — anything beyond it is creep.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly.\n\n{steps.diff.output}\n\nHunt for scope creep beyond openspec/changes/{args.change}/ and anything cuttable. One line per finding, format:\n`[severity] file:L<line>: <tag> <what>. <replacement>.`\nTags: delete: (dead code, speculative feature — replacement: nothing) | stdlib: (hand-rolled stdlib — name the function) | native: (dependency/code doing what the platform does — name the feature) | yagni: (abstraction with one implementation, config nobody sets) | shrink: (same logic, fewer lines — show the shorter form).\nA single smoke test / assert self-check is the minimum, never flag it for deletion.\n\nEnd with `net: -<N> lines possible.` If nothing to cut, output exactly: No findings."
    },
    {
      "id": "review-kiss-glm",
      "type": "agent",
      "agent": "critic",
      "model": "zai/glm-5.2",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Over-engineering review of this change. Scope: complexity ONLY — correctness and security belong to other reviewers. The attached proposal.md defines the requested scope — anything beyond it is creep.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly.\n\n{steps.diff.output}\n\nHunt for scope creep beyond openspec/changes/{args.change}/ and anything cuttable. One line per finding, format:\n`[severity] file:L<line>: <tag> <what>. <replacement>.`\nTags: delete: (dead code, speculative feature — replacement: nothing) | stdlib: (hand-rolled stdlib — name the function) | native: (dependency/code doing what the platform does — name the feature) | yagni: (abstraction with one implementation, config nobody sets) | shrink: (same logic, fewer lines — show the shorter form).\nA single smoke test / assert self-check is the minimum, never flag it for deletion.\n\nEnd with `net: -<N> lines possible.` If nothing to cut, output exactly: No findings."
    },
    {
      "id": "review-sec-claude",
      "type": "agent",
      "agent": "security-reviewer",
      "model": "anthropic/claude-opus-4-8",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Security review of this change. Optimize for RECALL — surface anything suspicious, including cross-file and logic-level issues; a second reviewer will filter noise. The attached proposal.md tells you the INTENT of the change.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly.\n\nWork plan: FIRST list the risk points in this diff (one line each), THEN investigate each with your tools before reporting findings.\n\n{steps.diff.output}\n\nHunt for: command/shell injection, unsafe eval/exec, path traversal, secret or credential leaks, missing input validation, authorization gaps (IDOR), unsafe defaults, and data exposure.\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If clean, output exactly: No findings."
    },
    {
      "id": "review-sec-gpt",
      "type": "agent",
      "agent": "security-reviewer",
      "model": "openai-codex/gpt-5.5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "dependsOn": [
        "diff",
        "review-rules"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md"
      ],
      "task": "Security review of this change. Optimize for PRECISION — report only findings you are confident are real and exploitable; classify each by OWASP category. The attached proposal.md tells you the INTENT of the change.\n\nReview rules:\n{steps.review-rules.output}\n\nThe pasted diff may be truncated — you have read/grep/ls, so read the changed files directly.\n\nWork plan: FIRST list the risk points in this diff (one line each), THEN investigate each with your tools before reporting findings.\n\n{steps.diff.output}\n\nHunt for: command/shell injection, unsafe eval/exec, path traversal, secret or credential leaks, missing input validation, authorization gaps (IDOR), unsafe defaults, and data exposure.\n\nOutput a numbered list of findings, each tagged [blocker], [major], or [minor]. If clean, output exactly: No findings."
    },
    {
      "id": "verdict",
      "type": "agent",
      "agent": "final-arbiter",
      "model": "anthropic/claude-fable-5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "dependsOn": [
        "build-test",
        "spec-validate",
        "diff",
        "review-spec-claude",
        "review-spec-kimi",
        "review-bugs-claude",
        "review-bugs-gpt",
        "review-kiss-gpt",
        "review-kiss-glm",
        "review-sec-claude",
        "review-sec-gpt"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md",
        "openspec/changes/{args.change}/tasks.md"
      ],
      "task": "You are the final arbiter reviewing the current working tree against OpenSpec change `{args.change}`. Consolidate the evidence below into ONE report.\n\nReviewer findings are CLAIMS, not facts. You have read/grep/ls: before confirming any [blocker] or [major] finding, VERIFY it against the actual code — open the file, check the lines, confirm the problem reproduces in source. Reject findings that do not hold up, however confident they sound.\n\nBuild/test output (ends with VERIFY_EXIT=<code>; non-zero means tests failed):\n{steps.build-test.output}\n\nSpec validation (ends with VALIDATE_EXIT=<code>):\n{steps.spec-validate.output}\n\nDiff:\n{steps.diff.output}\n\nSpec-compliance review (Claude):\n{steps.review-spec-claude.output}\n\nSpec-compliance review (Kimi):\n{steps.review-spec-kimi.output}\n\nCorrectness review (Claude, recall-tuned):\n{steps.review-bugs-claude.output}\n\nCorrectness review (GPT, precision-tuned):\n{steps.review-bugs-gpt.output}\n\nSimplicity review (GPT):\n{steps.review-kiss-gpt.output}\n\nSimplicity review (GLM):\n{steps.review-kiss-glm.output}\n\nSecurity review (Claude, recall-tuned):\n{steps.review-sec-claude.output}\n\nSecurity review (GPT, precision-tuned):\n{steps.review-sec-gpt.output}\n\nRules:\n- An empty review section means that reviewer was skipped — note it, do not treat silence as approval.\n- Mechanical snippet check: every finding must quote verbatim code from the cited file. Grep the quote in that file (whitespace-insensitive); no match = REJECT the finding outright.\n- Coverage audit: cross-check each reviewer's `Coverage:` section against the FILES CHANGED manifest in the diff. A file skipped or unmentioned by ALL reviewers of an angle is an uncovered gap — list it in the report; never treat silence as reviewed.\n- Where the two reviewers of one angle disagree, adjudicate explicitly against the code and say which one you believe and why.\n- VERIFY_EXIT or VALIDATE_EXIT non-zero MUST result in BLOCK. Also block on any verified [blocker] finding, on unimplemented requirements, or on tests weakened to pass. Use judgment on [major]/[minor] — cite why you accept or reject each.\n\nStructure: verdict first, then confirmed findings (with file:line, each marked VERIFIED after your own code check), then rejected findings with one-line reasons, then skipped reviewers. End with exactly `VERDICT: PASS`, or `VERDICT: BLOCK` followed by a precise numbered fix list. If uncertain, BLOCK."
    },
    {
      "id": "fix",
      "type": "agent",
      "agent": "executor-code",
      "thinking": "xhigh",
      "idempotent": false,
      "dependsOn": [
        "verdict"
      ],
      "context": [
        "openspec/changes/{args.change}/proposal.md",
        "openspec/changes/{args.change}/tasks.md"
      ],
      "task": "You are the fix stage of the review flow for OpenSpec change `{args.change}`.\n\nArbiter verdict:\n{steps.verdict.output}\n\nIf the verdict ends with `VERDICT: PASS`: change NOTHING and output exactly: NOTHING_TO_FIX.\n\nOtherwise: fix every item in the numbered fix list in the working tree. Keep diffs minimal and scoped to the findings. For every confirmed BUG you fix, add or extend a test that fails before and passes after the fix (skip only if an existing test already covers it — say which). Never weaken or delete tests to make them pass — if you believe a finding is wrong, do not 'fix' it; dispute it in your output instead. Run `{args.verify}` and iterate until it passes.\n\nNote: this phase may run multiple rounds — earlier rounds' edits are already in the working tree; check what is already fixed before redoing work.\n\nOutput a numbered list of the fixes you applied (with file:line), tests you added, plus any findings you dispute and why."
    },
    {
      "id": "recheck",
      "type": "script",
      "dependsOn": [
        "fix"
      ],
      "run": [
        "bash",
        "-c",
        "set -o pipefail; ({args.verify}); code=$?; echo; echo VERIFY_EXIT=$code; echo '--- working tree ---'; git status --short; exit 0"
      ],
      "timeout": 300000
    },
    {
      "id": "confirm",
      "type": "gate",
      "agent": "verifier",
      "model": "openai-codex/gpt-5.5",
      "thinking": "xhigh",
      "tools": [
        "read",
        "grep",
        "ls"
      ],
      "optional": true,
      "onBlock": "retry",
      "retry": {
        "max": 2
      },
      "dependsOn": [
        "fix",
        "recheck"
      ],
      "eval": [
        "{steps.fix.output} contains NOTHING_TO_FIX"
      ],
      "task": "You verify the fix round of a review flow.\n\nFix stage claims:\n{steps.fix.output}\n\nRe-check output (VERIFY_EXIT=0 means tests pass):\n{steps.recheck.output}\n\nCheck: (1) VERIFY_EXIT is 0; (2) each claimed fix actually exists in the working tree — read the cited files and confirm; (3) claimed new tests exist and are not weakened versions of old ones. Disputed findings are acceptable if the dispute is reasoned — do not block on them.\n\nEnd with exactly `VERDICT: PASS`, or `VERDICT: BLOCK` followed by a precise numbered list of what remains broken or unfixed."
    },
    {
      "id": "summary",
      "type": "agent",
      "agent": "doc-writer",
      "dependsOn": [
        "verdict",
        "fix",
        "recheck",
        "confirm"
      ],
      "final": true,
      "task": "Write the final review report for `{args.change}` for a human. Use the user's or session's language when available; otherwise use English. Keep file paths, code identifiers, command names, and the `VERDICT:` line unchanged.\n\nArbiter verdict:\n{steps.verdict.output}\n\nFix stage output:\n{steps.fix.output}\n\nRe-check after fixes (VERIFY_EXIT=0 means tests pass):\n{steps.recheck.output}\n\nConfirm gate (verified the fixes against the code; empty means it was skipped or still blocking after retries — treat that as NEEDS ATTENTION):\n{steps.confirm.output}\n\nStructure: overall outcome first (clean pass / fixed & verified / needs attention), confirmed findings, fixes applied + tests added (or none), disputed findings, skipped reviewers. If fixes were applied, recommend re-running `/tf:openspec-review change={args.change}` for a fresh panel pass. Close with the next steps: `/plannotator-review`, then `openspec archive {args.change}`."
    }
  ]
}